import { BinaryMpdResponse } from './BinaryMpdResponse';

/** Reply of `albumart` — the cover image sitting beside the file (cover.jpg, folder.png, …). */
export class AlbumArtMpdResponse extends BinaryMpdResponse {}
